# demo2.py — 10 m 四方を延々と巡回する四足歩行（ANYmal-C）

Isaac Lab 6.0 系で、**学習済みの歩行ポリシー（rsl_rl の `policy.pt`）を読み込んだ ANYmal-C が、
10 m 四方の正方形コースを止まらずに周回し続ける**デモです。

`demo.py` が「ゴールに着いたら止まる片道ルート」なのに対し、こちらは
**ウェイポイントをリング状に巡回し、転倒・タイムアウトでリセットされない限り走り続ける**構成です。
長時間放置して歩行の安定性を眺めるのに向いています。

`demo.py` と骨格はほぼ同じなので、先に `demo.md` を読むと理解が早いです。

---

## 1. 起動方法

```powershell
# 仮想環境をアクティベート
PS C:\Users\takuy\dev> env_isaaclab\Scripts\activate

# プロジェクトへ移動して実行
PS C:\Users\takuy\dev\IsaacLab> .\isaaclab.bat -p demo2.py --viz kit
```

学習済みモデル（`policy.pt`）が必須です。既定パスは

```
logs/rsl_rl/anymal_c_rough/2026-07-18_16-38-49/exported/policy.pt
```

で、`IsaacLab` フォルダからの相対パスとして解決されます。見つからないと
`FileNotFoundError` で即終了します。

### コマンドラインオプション

| オプション | 既定値 | 内容 |
|---|---|---|
| `--model_path` | `logs/rsl_rl/anymal_c_rough/2026-07-18_16-38-49/exported/policy.pt` | TorchScript 形式の学習済みポリシー |

`--viz` / `--device` などは Isaac Lab 標準の `AppLauncher` 引数です。
`demo.py` と違い `headless` を上書きしていないので、`--viz none` でビューアなし実行もできます。

---

## 2. 何をするデモか

1. `IsaacContrib-Velocity-Rough-AnymalC` タスクの設定を読み込み、地形を**完全な平面**に置き換える
2. 学習済み歩行ポリシーをロードする
3. 正方形の 4 隅を順に追いかける。最後の隅まで行ったら**先頭に戻ってループ**する

### 巡回コース（単位 m）

```
        y
        ^
  10.0  |  ●(0,10) <-------------------------- ●(10,10)
        |    |                                     ^
        |    |                                     |
        |    |          巡回方向：反時計回り        |
        |    v                                     |
   0.0  |  ◎(0,0) --------------------------> ●(10,0)
        +-------------------------------------------> x
          0.0                                    10.0
```

- ウェイポイントは `[(10,0), (10,10), (0,10), (0,0)]` の順で、**インデックスを剰余で回して**無限ループします。
- ロボットのスタート地点は原点 (0, 0)。最初の目標は (10, 0) なので、まず +x 方向へ歩き出します。
- 到達判定は**距離 0.6 m 以内**。到達した瞬間に次の目標へ切り替え、その場で距離・方向も再計算します。
- 障害物はありません。`demo.py` のコーンに相当するものは置いていません。

コースの角は **90 度ターン**なので、ここが転倒のリスクが最も高い箇所です。
後述の平滑化フィルタは、この角を曲がりきるために入っています。

---

## 3. フィジカルAI部分

構造は `demo.py` と同じで、毎ステップ次を回します。

### (1) 自己位置の取得

```python
root_pos_tensor  = wp.to_torch(robot.data.root_pos_w)   # 世界座標 [m]
root_quat_tensor = wp.to_torch(robot.data.root_quat_w)  # 姿勢（x, y, z, w）
_, _, root_yaw = euler_xyz_from_quat(root_quat_tensor)
```

Isaac Lab 6.0 のアセットデータは Warp の `ProxyArray` なので、`wp.to_torch()` で
PyTorch テンソルへ変換してから使います。四元数は **(x, y, z, w) 順**です。

### (2) 経路追従（比例制御）

```
dx, dy      = 目標 − 現在地
distance    = hypot(dx, dy)
target_yaw  = atan2(dy, dx)
yaw_error   = atan2(sin(target_yaw − yaw), cos(target_yaw − yaw))   # [-π, π] に正規化
```

- **前進速度**: `0.45 × max(cos(yaw_error), 0)` [m/s]
  → 目標を正面に捉えているほど速く、横を向いている間はほぼ 0。**向き直ってから進む**挙動です。
- **旋回速度**: `1.2 × yaw_error` を `±0.45 rad/s` でクリップ。

`demo.py` との違いは、**ウェイポイント切り替え時に `dx, dy, distance` をその場で再計算している**点です。
切り替えた直後のステータス表示が「古い目標までの残り距離」にならないようにしています。

### (3) コマンドの平滑化（ローパスフィルタ）

```
smoothed ← α × raw + (1 − α) × smoothed      （α = 0.08）
```

このデモの主題です。正方形コースの角では目標方向が**一瞬で 90 度変わる**ため、
旋回指令が階段状に跳ね上がります。そのままポリシーに渡すと ANYmal-C は踏ん張れず転倒します。
指数移動平均で 1 次遅れをかけ、**約 12 ステップかけて新しい指令に寄せていく**ことで、
角を膨らみながら滑らかに曲がります。

（α = 0.08 は「1 ステップで差の 8 % を詰める」という意味です。時定数はおよそ 1/α ≒ 12 ステップ。）

### (4) コマンドマネージャへの注入

```python
cmd_term.cfg.ranges.lin_vel_x = (smoothed_v_x, smoothed_v_x)
cmd_term.cfg.ranges.lin_vel_y = (0.0, 0.0)
cmd_term.cfg.ranges.ang_vel_z = (smoothed_v_z, smoothed_v_z)
cmd_term._resample(env_ids)
```

レンジの min と max を同じ値にすることで、ランダムサンプリングが**必ずその値を返す**ようになります。
あわせて起動時に `ranges` をすべて `(0.0, 0.0)` に初期化し、
`resampling_time_range = (10000.0, 10000.0)` で**環境が勝手にコマンドを引き直さない**ようにしています。

### (5) ポリシー推論とステップ実行

```python
with torch.no_grad():
    actions = policy(obs["policy"])
obs, rewards, dones, infos = env.step(actions)
simulation_app.update()
```

`dones[0]` が立った（＝転倒などでリセットされた）場合は、
ウェイポイントのインデックスと平滑化バッファを初期化してコースの最初からやり直します。

---

## 4. 実行したときのログ

起動時:

```
[INFO] タスク設定を読み込んでいます: IsaacContrib-Velocity-Rough-AnymalC
[INFO] エピソード制限時間を延長し、タイムアウトによるリセットを無効化します。
[INFO] 地形設定を【完全な平坦（Plane）】に変更し、でこぼこを消去します。
[INFO] クラス設定を適用してGym環境を生成中...
[INFO] モデルを読み込んでいます: logs/rsl_rl/anymal_c_rough/.../policy.pt
[INFO] シミュレーション開始。10m四方の滑らかな巡回ルート制御を実行します...
```

走行中は 1 行ステータスが `\r` で更新され続けます:

```
[TRACKING] 目標WP:1 | 残り:4.83m | 角度誤差:0.02 | 入力(前進:0.45, 旋回:0.02)
```

角を曲がるとき:

```
[INFO] 🚩 ウェイポイント通過！次の目標 2: (0.0, 10.0) に向かいます。
```

転倒などでリセットされたとき:

```
[WARN] ⚠️ 環境のリセットを検知しました。目標ウェイポイントをスタート位置に初期化します。
```

**終了条件がありません**（周回し続けます）。止めるにはウィンドウを閉じるか `Ctrl+C` です。

---

## 5. コードの構成

`demo2.py` は関数分割せず `main()` にすべて書かれています。

| 段階 | 内容 |
|---|---|
| 引数パース → `AppLauncher` | **`isaaclab.app` 以外の import より前**に済ませる必要があります |
| `parse_env_cfg(task_name)` | タスク設定の取得 |
| 設定の書き換え | `episode_length_s` 延長 / 地形を `plane` に / `terrain_levels` カリキュラム削除 / コマンドレンジを 0 初期化＋自動更新停止 / `num_envs=1` / `use_fabric=True` / `viewer.env_index=0` |
| `gym.make` → `RslRlVecEnvWrapper` | rsl_rl 形式の観測を返すラッパ |
| `torch.jit.load` | TorchScript ポリシーのロード |
| メインループ | 自己位置取得 → 経路追従 → 平滑化 → コマンド注入 → 推論 → `env.step()` → `simulation_app.update()` |

設定書き換えはすべて `hasattr()` で存在確認してから行うため、
タスク設定の構造が変わってもスクリプトが落ちにくくなっています。

### `demo.py` との差分

| | `demo.py` | `demo2.py` |
|---|---|---|
| 経路 | 4 点を通って (10,10) で**停止** | 正方形 4 隅を**無限巡回** |
| 障害物コーン | あり（見た目のみ） | なし |
| `headless` | `False` に強制上書き | 上書きしない（`--viz none` が効く） |
| コマンドレンジの初期化 | なし | 起動時に `(0.0, 0.0)` を明示 |
| WP 切替時の距離再計算 | しない | する |
| `simulation_app.update()` | コメントアウト | 呼ぶ |
| 到達フラグ `arrived` | あり | なし（止まらない） |

---

## 6. ハマりどころ（実装メモ）

### ① 平滑化なしでは角で必ず転ぶ

90 度ターンの瞬間に `yaw_error` が ±π/2 まで跳ね、旋回指令がクリップ値いっぱいまで一気に立ち上がります。
ANYmal-C は脚の接地タイミングが間に合わず転倒します。
`filter_alpha` を 0.3 程度まで上げるだけでも転びやすくなるので、**小さめ（0.08）が安全側**です。

### ② コマンドレンジは起動時にも 0 にしておく

`env.reset()` の時点で環境がコマンドをサンプリングします。レンジを初期化していないと、
**最初の数ステップだけランダムな速度で歩き出します**。
`demo2.py` は `lin_vel_x / lin_vel_y / ang_vel_z` をすべて `(0.0, 0.0)` にしてから `gym.make` しています。

### ③ `resampling_time_range` を伸ばさないと勝手に上書きされる

`CommandManager` は一定時間ごとにコマンドを引き直します。
`(10000.0, 10000.0)` にして、**自分が `_resample()` を呼んだときだけ**値が変わるようにします。

### ④ 速度コマンドは「レンジを潰して再サンプル」で注入する

値を直接代入する公開 API がないため、`ranges` の min = max にして `_resample()` を呼びます。
`_resample()` は**アンダースコア始まりの内部 API** なので、バージョン更新で壊れる可能性があります。

### ⑤ タイムアウトで勝手にリセットされる

既定のエピソード長は 20 秒程度で、1 周する前に打ち切られます。
`episode_length_s = 10000.0` で実質無限にしています。
平面地形では意味のない `terrain_levels` カリキュラムも削除しています。

### ⑥ import の順番（`AppLauncher` より前に Isaac 系を import しない）

`AppLauncher` で Omniverse アプリを起動する**前**に `gymnasium` や `isaaclab_tasks` を import すると、
拡張がロードされておらず失敗します。「引数パース → `AppLauncher` → それ以降に import」の並びは必須です。

### ⑦ 四元数は (x, y, z, w) 順

Isaac Lab 6.0 の `root_quat_w` は **XYZW** 順です。`euler_xyz_from_quat()` も XYZW 前提なのでそのまま渡せます。
古い資料の WXYZ 前提のコードを持ち込むと向きが壊れます。

---

## 7. 触ってみると面白いパラメータ

| 変えるもの | どうなるか |
|---|---|
| `waypoints`（コード内） | 三角形や八角形など、任意の巡回コースにできる |
| `filter_alpha = 0.08` | 大きくすると角をキビキビ曲がるが転倒しやすい。小さくすると大きく膨らむ |
| `0.45 * forward_factor` | 前進速度の上限 [m/s]。上げると角で曲がりきれなくなる |
| `torch.clamp(..., -0.45, 0.45)` | 旋回速度の上限 [rad/s]。上げると転倒しやすい |
| `distance < 0.6` | 到達判定の半径 [m]。大きくすると角を大きくショートカットする |
| `terrain_type = "plane"` | `"generator"` に戻すと起伏地形になり、歩行ポリシー本来の難易度になる |
| `episode_length_s` | 短くするとタイムアウトからの復帰挙動を観察できる |

---

## 8. 既知の制限

- **環境数は 1 固定**（`num_envs = 1`）。制御が Python のスカラー演算なので並列化にはテンソル化が必要です。
- **終了条件がありません**。周回し続けるので手動で止めます。
- 障害物・センサは一切ありません。純粋な経路追従のデモです。
- 到達判定が距離ベースなので、**行き過ぎたことを検知できません**
  （軸方向の符号付き距離で判定する方法は `demo5.md` §6 ② を参照）。
- 学習済み `policy.pt` に依存します。観測の次元が違うポリシーを渡すと推論で落ちます。
- `cmd_term._resample()` という内部 API に依存しています。

---

## 9. 参考

- タスク: `IsaacContrib-Velocity-Rough-AnymalC`（`isaaclab_tasks` に登録）
- 学習側のスクリプト: `scripts/reinforcement_learning/rsl_rl/train.py` / `play.py`
- 関連デモ:
  - `demo.md` — 同じ仕組みで障害物を避けて目標地点まで歩く版
  - `demo5.md` — LiDAR で障害物を実際に検知して避ける版
