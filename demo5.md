# demo5.py — 倉庫AGV搬送シミュレーション（フィジカルAIデモ）

Isaac Lab 3.0 系で、**倉庫内のAGV（無人搬送車）がパレット状の荷物を運ぶ**デモです。
AGV は Isaac Sim の公式アセットに入っている **idealworks 社の `iw.hub`**（実在の物流AGV）をそのまま使います。

「知覚 → 判断 → 制御」を毎ステップ回すクローズドループになっていて、特に
**荷台の上で荷物が滑ったことを検知して、自分の速度・加速度の上限をオンラインで学習し直す**
ところがこのデモの主題です。

---

## 1. 起動方法

```powershell
# 仮想環境をアクティベート
PS C:\Users\takuy\dev> env_isaaclab\Scripts\activate

# プロジェクトへ移動して実行（ビューアあり・デフォルト2サイクル）
PS C:\Users\takuy\dev\IsaacLab> .\isaaclab.bat -p demo5.py --viz kit
```

| やりたいこと | コマンド |
|---|---|
| ビューアで眺める（標準） | `isaaclab.bat -p demo5.py --viz kit` |
| 画面なしでログだけ見る | `isaaclab.bat -p demo5.py --viz none --cycles 1` |
| 適応制御を切って比較する | `isaaclab.bat -p demo5.py --viz kit --no_adaptive` |
| 重い荷物・もっと滑る荷台 | `isaaclab.bat -p demo5.py --viz kit --payload_mass 40 --deck_friction 0.08` |

初回起動時は AGV の USD をクラウドアセット（`omniverse-content-production.s3...`）から取得するので、
ネットワーク接続が必要です。2回目以降はローカルキャッシュが使われます。

### コマンドラインオプション

| オプション | 既定値 | 内容 |
|---|---|---|
| `--cycles` | `2` | 搬送サイクル数（ピッキング棚 → 搬出棚 → 戻る、で1サイクル） |
| `--cruise_speed` | `1.4` | 通路走行時の公称速度 [m/s] |
| `--payload_mass` | `20.0` | 荷物の質量 [kg] |
| `--deck_friction` | `0.12` | 荷台と荷物のあいだの摩擦係数（**物理側の真値**。小さいほど荷崩れしやすい） |
| `--friction_prior` | `0.30` | コントローラが最初に思い込んでいる摩擦係数（**推定の初期値**） |
| `--no_adaptive` | off | 積載適応制御を無効化する（学習せず、初期の思い込みのまま走る） |
| `--visual_mode` | `proxy` | AGV の描画方法。`proxy` = 当たり判定形状で描く（既定）、`highres` = USD 付属の高解像度メッシュで描く（§9 参照） |
| `--max_steps` | `60000` | 安全のためのステップ数上限（到達したら打ち切ってサマリを出す） |

`--viz` / `--device` などは Isaac Lab 標準の `AppLauncher` 引数です。
`--headless` も使えますが非推奨警告が出るので `--viz none` を推奨します。

---

## 2. 何をするデモか

実機の iw.hub と同じ **「荷物の下に潜り込んで、天板を持ち上げて運ぶ」** 方式を物理で再現しています。

1. ピッキング棚（スタンドの上に荷物が載っている）へ通路を走って向かう
2. スタンドの間に**まっすぐ進入**して、荷物の真下に荷台を合わせる（ドッキング）
3. **リフト（昇降天板）を 40 mm 上げて**、荷物をスタンドから持ち上げる
4. 荷物を載せたまま倉庫を半周して搬出棚へ（途中の障害物は LiDAR で回避）
5. 搬出棚に進入して**リフトを下げ、荷物をスタンドに渡す**
6. 反対側の通路を通ってピッキング棚へ戻る（新しい荷物が補充される）→ 1へ

指定サイクル終わると、走行距離・荷ズレ回数・学習結果などのサマリを表示して終了します。

### 倉庫レイアウト（単位 m）

```
        y
        ^
  +5.6  |=================== 北壁 ===================|
        |                                             |
  +3.4  |   [搬出棚]◀━━━━━━━━━━━━━━━━━━━●(8.0,3.4)    |
        |    (4.0,3.5)                       ╲        |
  +2.6  |  ●(-1.8,2.6)                        ●(9.3,2.6)
        |   │                                  │      |
   0.0  |   │      ▓▓▓▓ 中央棚 ▓▓▓▓            │■     |   ■ = 置き去りパレット
        |   │      (4.0,0) 5.0 x 1.2           │(9.6,0)|
  -2.6  |  ●(-1.8,-2.6)                       ●(9.3,-2.6)
        |    ╲                                 │      |
  -3.4  |     ●(0.0,-3.4) ━━━▶[ピッキング棚]━━━●(8.6,-3.4)
        |      ↑AGVスタート    (4.0,-3.5)              |
  -5.6  |=================== 南壁 ===================|
        +---------------------------------------------> x
       -3.8                  4.0                    11.8
```

- `●` … 経路のウェイポイント（ざっくりした「地図上の経路」）
- AGV は**右回りのループ**を走ります。中央棚があるので直線では行けません。
- **置き去りパレット**は左右の通路のウェイポイントを結ぶ直線上に置いてあり、
  LiDAR で見つけて避けないと通れません（＝回避のデモ）。
- 外壁があるので、パレットの「狭いほう」をすり抜けずに「広いほう」へ回り込みます。
- ステーションのスタンド（荷物の台）は **LiDAR の障害物には含めていません**。
  実機のAGVも「地図に載っているドッキング先」には突っ込んでいくので、それに合わせています。

### ステーションの構造（断面）

```
        荷物 (1.0 x 1.1 x 0.4 m, 20 kg)
     ┌──────────────────────────┐
     │                          │   ← 底面 0.25 m
  ▒▒▒│▒▒▒                    ▒▒▒│▒▒▒  スタンド（y = ±0.45、高さ 0.25）
     └──────────────────────────┘
        ┌────────────────┐
        │ AGV 荷台       │   下げた状態 0.229 m ／ 上げた状態 0.269 m
        │ (幅 0.65 m)    │   ⇒ 下げていれば 21 mm の隙間で下に潜り込める
        └────────────────┘      上げるとスタンドから 19 mm 浮いて荷物を奪える
```

AGV の車体幅は 0.65 m、スタンドの内側は ±0.39 m なので**そのまま通り抜けられます**。
だから入った側と反対側へ「走り抜けながら」積み下ろしができ、バック走行が要りません。

---

## 3. フィジカルAI部分（このデモの中心）

毎ステップ、次の 4 つを回しています。

### (1) 知覚 — 2D LiDAR

- `MultiMeshRayCaster` を車体前端（前方 0.30 m、地上高 0.14 m）に取り付け
- 水平 ±100°、2° 刻み（101本）、最大 8 m
- 荷台より下に付けているので、**荷物を積んでも視界が塞がりません**
- レイの当たった点から距離を計算し、当たらなかったレイは最大距離に丸めます

### (2) 判断・経路 — VFH 系の障害物回避

`LidarScan.pick_heading()`:

1. 各ヒットは、距離が近いほど広い角度範囲を塞ぐと考える
   → 塞ぐ半角 `asin(AVOID_RADIUS / 距離)`
2. 膨張半径 `AVOID_RADIUS = 0.85 m` は**荷物を積んだ状態の実効幅**（荷物の半幅 0.55 m ＋ 余裕）
3. `AVOID_LOOKAHEAD = 3.5 m` より遠いヒットは「まだ避けなくていい」として無視（アクティブウィンドウ）
4. 残った「空いている方向」のうち、**目標方向にいちばん近いもの**を選ぶ
5. 全部塞がっていたら、その場でいちばん奥まで抜けている方向へ向き直る

速度は「目標方向をどれだけ向いているか（`cos`）」と「前方クリアランス」で決めます。

### (3) 判断・積載 — 摩擦係数のオンライン推定（`PayloadAwareLimiter`）

荷台は滑りやすく（既定 μ = 0.12）、強く加減速・旋回すると荷物が滑ります。
コントローラは荷物が**どれだけ荷台の上でずれたか**を監視して、自分の限界を学習し直します。

- **荷物にかかる水平加速度**は、荷台が回転中心より `DECK_CENTER_X = -0.256 m` 後ろにあることを考慮して計算します:

  ```
  a_x = a_前後 − ω² · r        （遠心力の成分）
  a_y = v · ω  + α · r        （旋回 ＋ 角加速度の成分, r = −0.256）
  a   = hypot(a_x, a_y)
  ```

  ここで `α`（ヨー角加速度）を入れているのが重要です。**ハンドルを急に切り返すと、
  速度が低くても荷物は横に振られます**。

- **滑りの検知**: 積み込み時の相対位置を基準に、そこから `SLIP_WARN = 3 cm` 以上「さらに」
  ずれ、かつ前回イベントから `SLIP_COOLDOWN = 1.5 s` 経っていたら 1 イベントとして記録。
  （基準をリセットしない＋クールダウンを入れることで、荷物がカタカタ揺れているだけで
  何十回も誤検知するのを防いでいます）

- **推定の更新**: 滑りは加速した「あと」に見つかるので、検知時点ではなく
  **直前区間のピーク加速度**を「そのとき超えた限界」とみなします:

  ```
  μ推定 ← max(0.05, min(μ推定 × 0.8, ピーク加速度 / g))
  許容加速度 = 0.5 × μ推定 × g        （安全率 0.5）
  ```

  しばらく滑らなければ、μ推定はゆっくり初期値へ戻ります（速度を取り戻す）。

- **指令の制限**: 許容加速度から、
  - 旋回中の速度上限 `v ≤ a許容 / |ω|`
  - 前後加速度のレートリミット
  - ヨーレートの変化率上限 `|dω/dt| ≤ a許容 / |r|`

  の3つを課します。

### (4) 制御 — 差動二輪 ＋ リフト

```
ω_左 = (v − ω·b) / R,   ω_右 = (v + ω·b) / R      （R = 0.08 m, b = 0.28963 m）
```

車輪は速度制御（stiffness = 0, damping = 25, トルク上限 30 N·m）、
リフトは位置制御（過減衰: stiffness 1.5e5 / damping 3.0e4）で、指令は 0.03 m/s でランプさせます。

---

## 4. 実行したときのログ

```
[MISSION] ピッキング棚 に進入します (荷物の下に潜り込む)
[MISSION] ドッキング完了。リフトを上げて荷物を持ち上げます
[MISSION] 積載完了。搬送先へ向かいます
[PAYLOAD] 荷物が 8.9 cm ずれました (直前のピーク加速度 1.47 m/s^2)。 摩擦推定値を 0.150 に更新 → 許容加速度 0.73 m/s^2
[PAYLOAD] 荷物が 12.0 cm ずれました (直前のピーク加速度 1.19 m/s^2)。 摩擦推定値を 0.120 に更新 → 許容加速度 0.59 m/s^2
[PAYLOAD] 荷物が 16.3 cm ずれました (直前のピーク加速度 0.65 m/s^2)。 摩擦推定値を 0.066 に更新 → 許容加速度 0.33 m/s^2
[MISSION] 搬出棚 に進入します (荷物を降ろす)
[MISSION] 荷降ろし完了 (1/2 サイクル)
[SIM] 新しい荷物がピッキング棚に入庫しました
...（2サイクル目は [PAYLOAD] が一度も出ない）...
```

走行中は1行ステータスが `\r` で更新され続けます:

```
[GOTO_DROPOFF ] 位置:(  8.97,  2.03) 速度: 1.18m/s 指令: 1.16m/s 最近傍障害物: 2.7m@ -68deg 直進中 荷ズレ: 16.3cm mu推定:0.120
```

### 実測結果（既定パラメータ、RTX 5060Ti）

| | 適応制御 **あり**（2サイクル） | 適応制御 **なし** `--no_adaptive`（1サイクル） |
|---|---|---|
| 完了サイクル | 2 / 2 | 1 / 1 |
| 荷ズレ検知 | **3 回**（すべて1サイクル目） | 9 回 |
| 荷物の落下 | **0 回** | 2 回 |
| 荷物にかかった最大加速度 | 1.47 m/s² | 3.29 m/s² |
| 摩擦係数の推定（真値 0.12） | 0.30 → 0.15 → 0.12 → 0.066（最終 0.100） | 0.30 のまま |
| 走行距離 / 時間 | 58.8 m / 108 s | 23.2 m / 47 s |

**1サイクル目で学習し、2サイクル目は荷ズレ 0 回で運び切ります。**
`--no_adaptive` だと同じ経路で荷物が2回も荷台から落ちます（落ちたら自動で積み直して続行します）。

---

## 5. コードの構成

| 要素 | 役割 |
|---|---|
| `WarehouseSceneCfg` | シーン定義（地面・ライト・AGV・荷物・LiDAR）。`InteractiveSceneCfg` |
| `spawn_warehouse_props()` | 静的な倉庫設備（ステーションのスタンド、障害物、外壁）を `/World/Warehouse` 以下に配置 |
| `Station` | ドライブスルー型ステーション。ドッキング点・手前点・退出点、軸方向の符号付き距離を計算 |
| `MissionState` | 状態機械の状態（`GOTO_PICKUP` → `DOCK_PICKUP` → `LIFT_UP` → `EXIT_PICKUP` → …） |
| `TransportMission` | 状態遷移（`advance`）と各状態の走行指令（`drive_command`） |
| `LidarScan` | LiDAR の距離・方位への変換、前方クリアランス、回避方向の選択 |
| `PayloadAwareLimiter` | **摩擦係数のオンライン推定と、速度・加速度・ヨーレートの制限** |
| `wheel_targets()` | (v, ω) → 左右車輪の角速度 |
| `run_simulator()` | メインループ（知覚 → 状態機械 → 積載監視 → 制限 → 指令 → `sim.step()`） |

メインループの1ステップはこの順です:

```
自己位置・速度を読む → LiDAR 更新 → 状態機械を進める → 走行指令 (v, ω) を作る
  → 荷物の加速度を計算 → 滑り検知・摩擦推定の更新 → ω を制限 → v を制限
  → 車輪・リフトへ指令 → scene.write_data_to_sim() → sim.step() → scene.update()
```

物理は 60 Hz（`PHYSICS_RATE`）、制御も同じ 60 Hz、描画は 2 ステップに 1 回（`RENDER_INTERVAL = 2`）の
30 Hz です。追従カメラは**描画するフレームごと**に、一次ローパスをかけて動かしています。

---

## 6. ハマりどころ（実装メモ）

作るときに実際に詰まった点です。同じ構成で作るときの参考に。

### ① USD のジョイントドライブが `acceleration` 型だと車輪が差動しない

`iw.hub` の USD は車輪ドライブが `drive:angular:physics:type = "acceleration"` です。
この型ではゲインが**関節の実効慣性倍**されるため、Isaac Lab 側で `damping=25`, `effort_limit_sim=30`
と書いても実トルクは 1 N·m 程度にしかならず、**AGV が直進しかできません**（左右に速度差を
与えてもほとんど回らない）。

```python
spawn=sim_utils.UsdFileCfg(
    usd_path=IW_HUB_USD_PATH,
    joint_drive_props=sim_utils.schemas.JointDriveBaseCfg(drive_type="force"),  # ← これ
)
```

力（トルク）型に変えると、指令どおり ±3.58 rad/s で車輪が回り、ヨーレートも追従します。

### ② ドッキング判定にユークリッド距離を使うと乗り越す

目標点までの距離で判定すると、**行き過ぎたことを検知できず**ステーションを突き抜けて
走り続けます。ステーションの軸方向に射影した**符号付き残距離**で判定してください
（`Station.along()` / `TransportMission.dock_error()`）。行き過ぎたら符号が負になるので、
微速で後退して戻ります。

### ③ 回避半径は「荷物を積んだ実効幅」で取る

車体幅（半幅 0.33 m）で膨張処理すると、車体は通れるが**はみ出した荷物が障害物を擦る**
隙間を通ろうとして、荷物を荷台から落とします。荷物の半幅 0.55 m ＋余裕で `0.85 m` にしています。

### ④ ウェイポイントを障害物の「近く」に置かない

膨張半径より近い位置にウェイポイントを置くと、**そこへは絶対に到達できない**のに
到達を待ち続けてデッドロックします（回避で止まったまま動かなくなる）。
障害物は「ウェイポイントとウェイポイントを結ぶ直線上」に置き、ウェイポイント自体は
十分離しておきます。保険として `STUCK_TIMEOUT = 10 s` 動けなければ次のウェイポイントへ
スキップする処理も入れてあります。

### ⑤ 滑りの検知は「基準をリセットしない ＋ クールダウン」

滑りを検知するたびに基準位置を更新すると、荷物が振動しているだけで何十回も再検知し、
摩擦推定値が下限まで落ちてしまいます（最初の実装では 1 走行で 103 回検知しました）。
積み込み時の基準は固定して、「そこからさらに 3 cm」かつ「前回から 1.5 秒以上」で 1 回と数えます。

### ⑥ 学習に使う加速度は「検知時点」ではなく「直前のピーク」

滑りは加速の**あと**に検出されます。検知した瞬間の加速度を使うと
「加速度 0.00 m/s² で滑った」という無意味な観測になり、推定値が壊れます。
前回イベントからのピーク加速度を憶えておいて、それを限界とみなします。

### ⑦ 高解像度メッシュがレンダラを壊すことがある

`iw_hub.usd` の高解像度メッシュを読み込むと、RTX ビューポートがドームライトの背景しか
描かなくなることがあります（物理は正常のまま）。`use_collision_shape_visuals()` で該当プリムを
`SetActive(False)` して読み込ませず、当たり判定形状を表示しています。
`MakeInvisible()` では**読み込み自体は走る**ため一瞬白くなります。非表示ではなく非アクティブ化が要点です。
詳細な切り分け結果は §9 を参照。

---

## 7. 触ってみると面白いパラメータ

| 変えるもの | どうなるか |
|---|---|
| `--deck_friction 0.05` | かなり滑る。学習しても低速でしか運べなくなる |
| `--deck_friction 0.35` | ほとんど滑らない。学習が起きず初期値のまま走り切る |
| `--friction_prior 0.12` | 最初から正解を知っている状態。1回も荷崩れせずに走る |
| `--payload_mass 40` | 荷物が重くなる（摩擦の限界加速度自体は質量に依らないので、挙動の主な変化はリフト・車輪の負荷側） |
| `--cruise_speed 2.0` | 通路を飛ばすので学習前の荷崩れが派手になる |
| `OBSTACLES`（コード内） | 障害物の配置を変える。ウェイポイントから 1.5 m 以上離すこと |
| `AVOID_RADIUS`（コード内） | 小さくすると障害物すれすれを通り、荷物を擦り落とすようになる |

---

## 8. 既知の制限

- **環境数は 1 固定**（`num_envs=1`）。並列化する場合はコントローラ側が Python のループで
  書かれているため、テンソル化が必要です。
- 経路は**固定ウェイポイント＋反応的回避**で、大域的なプランナ（A* など）は入っていません。
  袋小路には入らない配置にしてあります。
- 荷物が落ちた場合は、デモを止めないために荷台の上へ自動で積み直します（`[PAYLOAD] 落下`）。
- 起動時に `isaaclab_assets-0.4.2 / fourbar_pole.usda が見つからない` というエラーが出ますが、
  このデモとは無関係な拡張の初期化失敗で、動作に影響しません。
- 物理バックエンドは PhysX で動作を確認しています。
- AGV の見た目は既定で当たり判定形状です（§9 のレンダラ問題のため）。実機そっくりの外観で見たい場合は
  `--visual_mode highres` を試してください。

---

## 9. トラブルシューティング

### ビューアが「最初は映るのに1秒ほどで真っ白になる」

**原因は `iw_hub.usd` が参照している高解像度メッシュ（`HighResProps/*.usd`）です。**
これらがロードされると RTX レンダラがドームライトの背景しか描かなくなり、ビューポート全体が
ライトの色で塗りつぶされます（＝真っ白）。物理シミュレーションは正常に動き続けるので、
ターミナルのステータス行は普通に流れます。

対策済みです: 既定の `--visual_mode proxy` では、それらのメッシュを
`prim.SetActive(False)` で読み込み対象から外し、代わりに USD にもともと入っている
**当たり判定形状（箱とシリンダ）を表示**します。見た目はシンプルになりますが、
物理・寸法・挙動は完全に同じです（実測でログが1ステップも変わらないことを確認済み）。

切り分けと計測の記録:

| 試したこと | 結果 |
|---|---|
| フォローカメラ（`sim.set_camera_view`）を無効化 | 変わらず真っ白 |
| LiDAR のデバッグ表示 (`debug_vis`) を無効化 | 変わらず真っ白 |
| RTX の自動露出（`/rtx/post/histogram/enabled`）を無効化 | もともと無効。変わらず |
| `--rendering_mode performance` / `quality` | どちらも真っ白 |
| `use_fabric=False` | 変わらず真っ白 |
| ドームライトの強度を下げる | 白 → 一様な灰色になるだけ（＝ジオメトリが1つも描かれていない証拠） |
| カメラを 600 m 離す | 画像がまったく変化しない（レンダラ側が壊れている） |
| AGV を物理なしの「ただの USD」として置く | 真っ白 → **アセット単体が原因**と確定 |
| AGV の高解像度メッシュだけ非表示にする | **正常に描画**（安定） |
| メッシュは残してマテリアルだけ差し替える | 真っ白（マテリアルではなくメッシュ側の問題） |
| VRAM（`torch.cuda.mem_get_info`） | 2.3 GB 空き。容量不足ではない |

`--visual_mode highres` を付けると元のメッシュで描画します。環境によっては問題なく映るかもしれません。

### 動きがカクカクする

見た目の滑らかさは「**実時間1秒あたり何フレーム描けるか**」で決まります。このマシンでの実測は:

| 設定 | 実時間30秒ぶんの進み | 体感 |
|---|---|---|
| 物理120 Hz + 描画30 Hz | 0.63倍速 | 実時間では約19 fps → カクつく |
| 物理120 Hz + 描画60 Hz | 0.43倍速 | 約26 fps |
| **物理60 Hz + 描画30 Hz（現設定）** | **1.07倍速** | **約28 fps・ほぼ実時間** |

1フレームの描画に約23 ms かかるので、物理を120 Hzで回すと描画に回す時間が足りなくなります。
物理を60 Hzに落としても挙動は変わらないことを確認済みです（荷ズレ3回・落下0回・走行58.9 m で同一。
摩擦推定は 0.114 と、むしろ真値0.12に近づきました）。

もうひとつの原因は**追従カメラの更新頻度**でした。以前は20ステップ（6 Hz）に1回しかカメラを
動かしていなかったため、描画が滑らかでも視点が段階的に飛んでいました。現在は描画フレームごとに
更新し、`CAMERA_SMOOTHING` で滑らかに追従させています。

### その他、描画が重い・映らないとき

- 描画は `RENDER_INTERVAL = 2`（物理 60 Hz に対して 30 Hz）に間引いてあります。
  それでも重い場合は `4`（15 Hz）に上げ、逆に滑らかさを優先するなら `1` に下げてください
  （`1` にすると描画が増えるぶん、ミッションの進みが実時間より遅くなります）。
- 描画の問題かロジックの問題かを切り分けるには `--viz none --cycles 1` で実行します。
  サマリまで完走すればロジック側は問題ありません。
- GPU 認識はログの冒頭で確認できます（`| 0 | NVIDIA GeForce RTX 5060 Ti | Yes: 0 |` のような行）。
  Intel UHD Graphics が `Skipping unsupported non-NVIDIA GPU` と出るのは正常です。
- 初回起動は RTX のシェーダキャッシュ構築とアセットのダウンロードで時間がかかります。

### `isaaclab_assets ... fourbar_pole.usda` のエラーが出る

このデモとは無関係な拡張の初期化失敗です（`isaaclab_assets` が起動時に別ロボットのアセットを
探しにいって失敗している）。demo5 は `isaaclab_assets` を使っていないので影響ありません。

### AGV が直進しかしない／その場でぐるぐる回る

車輪ドライブが力型になっているか確認してください（§6 ①）。また、経路のウェイポイントが
障害物の膨張半径（`AVOID_RADIUS = 0.85 m`）の内側にないかも確認してください（§6 ④）。

---

## 10. 参考

- AGV アセット: `{ISAAC_NUCLEUS_DIR}/Robots/Idealworks/iwhub/iw_hub.usd`
  （車輪半径 0.08 m、車輪間隔 0.579 m、リフトのストローク 0〜0.04 m、車体質量 30 kg — すべて USD から読み取った値）
- 関連する Isaac Lab のチュートリアル:
  - `scripts/tutorials/02_scene/create_scene.py`（`InteractiveScene` の使い方）
  - `scripts/tutorials/01_assets/add_new_robot.py`（USD ロボットの追加と車輪速度制御）
  - `scripts/demos/sensors/multi_mesh_raycaster.py`（`MultiMeshRayCaster` の使い方）
