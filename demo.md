# demo.py — ウェイポイント経由で目標地点まで歩く四足歩行（ANYmal-C）

Isaac Lab 6.0 系で、**学習済みの歩行ポリシー（rsl_rl の `policy.pt`）を読み込んだ ANYmal-C が、
指定した経由地を順番に通って (10, 10) までたどり着く**デモです。

歩き方そのものは強化学習で学習済みのポリシーに任せ、スクリプト側は
**「どっちへ、どれくらいの速さで進むか」という速度コマンドだけを毎ステップ作って注入**します。
いわゆる「低レベル制御は RL、高レベルの経路は古典制御」という階層構成の最小例です。

---

## 1. 起動方法

```powershell
# 仮想環境をアクティベート
PS C:\Users\takuy\dev> env_isaaclab\Scripts\activate

# プロジェクトへ移動して実行
PS C:\Users\takuy\dev\IsaacLab> .\isaaclab.bat -p demo.py --viz kit
```

学習済みモデル（`policy.pt`）が必須です。既定パスは

```
logs/rsl_rl/anymal_c_rough/2026-07-18_16-38-49/exported/policy.pt
```

で、`IsaacLab` フォルダからの相対パスとして解決されます。見つからないと
`FileNotFoundError` で即終了します。別の学習結果を使う場合は `--model_path` で指定してください。

### コマンドラインオプション

| オプション | 既定値 | 内容 |
|---|---|---|
| `--model_path` | `logs/rsl_rl/anymal_c_rough/2026-07-18_16-38-49/exported/policy.pt` | TorchScript 形式の学習済みポリシー |

`--device` などは Isaac Lab 標準の `AppLauncher` 引数です。
ただし **`--headless` は効きません**（スクリプト内で `args_cli.headless = False` を上書きしているため）。§6 ① 参照。

---

## 2. 何をするデモか

1. `IsaacContrib-Velocity-Rough-AnymalC` タスクの設定を読み込み、地形を**完全な平面**に置き換える
2. 目印のコーンを 2 本（オレンジ色）立てる
3. 学習済み歩行ポリシーをロードする
4. 4 つのウェイポイントを順に追いかけ、最後のゴール (10, 10) に着いたらその場で停止する

### フィールドのレイアウト（単位 m）

```
        y
        ^
  10.0  |                                         ★ゴール (10.0, 10.0)
        |                                       /
   7.0  |                          ▲コーン2 (7.0, 7.0)
   6.0  |                               ●(8.0, 6.0)
        |                             /
   5.0  |             ●(5.0, 5.0) --/
   4.5  |  ●(2.5, 4.5)  \
        |   \            \
   3.5  |    \    ▲コーン1 (3.5, 3.5)
        |     \
   0.0  |  ◎スタート (0, 0)
        +--------------------------------------------------> x
          0.0      3.5        5.0       7.0      10.0
```

- `●` … 経由地（ウェイポイント）。`▲` … オレンジのコーン。
- (0,0) → (10,10) の直線上にコーンが 2 本あるので、**左上・右下へ振ってすり抜ける**経路にしてあります。
- 到達判定は**現在地と経由地の距離 0.6 m 以内**。到達すると次の経由地へ切り替わります。
- 最後の (10,10) に着くと `arrived = True` になり、速度コマンドを 0 にして停止します。

> **注意**: コーンは `AssetBaseCfg` + `ConeCfg`（`visual_material` のみ）で置いてあり、
> **当たり判定を持ちません**。つまり「見えているだけの目印」で、ロボットはぶつからずすり抜けます。
> このデモは「障害物を避ける経路をあらかじめ人間が与えた場合に、ポリシーがそのとおり歩けるか」を
> 見るものです。センサで障害物を検知して避けるデモは `demo5.md` を参照してください。
>
> （`README.md` には「LLM にルートを決めさせる」と書かれていますが、現在の `demo.py` に LLM 呼び出しはなく、
> ウェイポイントはソース内にハードコードされています。）

---

## 3. フィジカルAI部分

毎ステップ、次の流れを回しています。

### (1) 自己位置の取得

```python
root_pos_tensor  = wp.to_torch(robot.data.root_pos_w)   # 世界座標 [m]
root_quat_tensor = wp.to_torch(robot.data.root_quat_w)  # 姿勢（x, y, z, w）
_, _, root_yaw = euler_xyz_from_quat(root_quat_tensor)
```

Isaac Lab 6.0 のアセットデータは **Warp の `ProxyArray`** なので、`wp.to_torch()` で
PyTorch テンソルに変換してから使います。四元数は **(x, y, z, w) 順**です（§6 ③）。

### (2) 経路追従（比例制御）

```
dx, dy      = 目標 − 現在地
distance    = hypot(dx, dy)
target_yaw  = atan2(dy, dx)
yaw_error   = atan2(sin(target_yaw − yaw), cos(target_yaw − yaw))   # [-π, π] に正規化
```

- **前進速度**: `0.45 × max(cos(yaw_error), 0)` [m/s]
  → 目標を正面に捉えているほど速く、横や後ろを向いているときは 0 になります。
  つまり**まず向き直ってから進む**挙動になります。
- **旋回速度**: `1.2 × yaw_error` を `±0.45 rad/s` でクリップ
  → ゲインを上げるとキビキビ曲がりますが、四足歩行は**旋回が急だと転びます**。

### (3) コマンドの平滑化（ローパスフィルタ）

```
smoothed ← α × raw + (1 − α) × smoothed      （α = 0.08）
```

ウェイポイントを切り替えた瞬間、目標方向が一気に変わって旋回指令がステップ状に跳ねます。
これをそのままポリシーに渡すと ANYmal-C が踏ん張れず転倒するため、
**指数移動平均で 1 次遅れをかけて**なまします。α を大きくすると追従は速くなりますが転びやすくなります。

### (4) コマンドマネージャへの注入

学習済みポリシーの観測には「目標速度コマンド」が含まれています。
そこへ自分の計算値を入れるために、コマンド項の**レンジを上下同値にして再サンプリング**させます。

```python
cmd_term.cfg.ranges.lin_vel_x = (smoothed_v_x, smoothed_v_x)
cmd_term.cfg.ranges.lin_vel_y = (0.0, 0.0)
cmd_term.cfg.ranges.ang_vel_z = (smoothed_v_z, smoothed_v_z)
cmd_term._resample(env_ids)
```

レンジの min と max を同じ値にすることで、ランダムサンプリングが**必ずその値を返す**ようになります。

### (5) ポリシー推論とステップ実行

```python
with torch.no_grad():
    actions = policy(obs["policy"])
obs, rewards, dones, infos = env.step(actions)
```

`dones[0]` が立った（＝環境がリセットされた）場合は、ウェイポイントの進行状況と
平滑化バッファをすべて初期化してやり直します。

---

## 4. 実行したときのログ

起動時:

```
[INFO] タスク設定を読み込んでいます: IsaacContrib-Velocity-Rough-AnymalC
[INFO] 視覚用の障害物コーンを追加しました: 位置 (3.5, 3.5)
[INFO] 視覚用の障害物コーンを追加しました: 位置 (7.0, 7.0)
[INFO] クラス設定を適用してGym環境を生成中...
[INFO] モデルを読み込んでいます: logs/rsl_rl/anymal_c_rough/.../policy.pt
[INFO] シミュレーション開始。ウェイポイント経由で安全に目標へ向かいます...
```

走行中は 1 行ステータスが `\r` で更新され続けます:

```
[NAV] 【経由地 1 へ移動中】 残り距離:3.21m | 角度誤差:0.08 | 前進:0.44 | 旋回:0.09
```

経由地を通過したとき / ゴールしたとき:

```
[INFO] 🚩 経由地を通過！ 次の目標地点 2: (5.0, 5.0) へ向かいます。
[INFO] 🎉 ゴール地点 (10.0, 10.0) に到着しました！安全にその場で停止します。
```

転倒などで環境がリセットされたとき:

```
[WARN] ⚠️ 環境リセット検知。ルートをスタート地点からやり直します。
```

ゴール後もループは回り続けるので、終了はウィンドウを閉じるか `Ctrl+C` です。

---

## 5. コードの構成

`demo.py` は関数分割せず、`main()` にすべて書かれています。処理の順番は次のとおりです。

| 段階 | 内容 |
|---|---|
| 引数パース → `AppLauncher` | **`isaaclab.app` 以外の import より前**に済ませる必要があります（§6 ②） |
| `parse_env_cfg(task_name)` | タスク設定の取得 |
| 設定の書き換え | `episode_length_s` 延長 / 地形を `plane` に / `terrain_levels` カリキュラム削除 / コマンド自動更新の停止 / `num_envs=1` / `use_fabric=True` / `viewer.env_index=0` |
| コーンの追加 | `setattr(env_cfg.scene, f"cone_{i}", AssetBaseCfg(...))` で動的にシーンへ足す |
| `gym.make` → `RslRlVecEnvWrapper` | rsl_rl 形式の観測を返すラッパ |
| `torch.jit.load` | TorchScript ポリシーのロード |
| メインループ | 自己位置取得 → 経路追従 → 平滑化 → コマンド注入 → 推論 → `env.step()` |

設定書き換えはすべて `hasattr()` で存在確認してから行っているため、
タスク設定の構造が変わってもスクリプトが落ちにくくなっています。

---

## 6. ハマりどころ（実装メモ）

### ① `--headless` が効かない

スクリプトが `AppLauncher` を作る直前に `args_cli.headless = False` を強制しています。
ビューアなしで動かしたい場合はこの行を消してください。
なお Isaac Lab 6.0 では `--headless` より **`--viz none`** が推奨です（`--headless` は非推奨警告が出ます）。

### ② import の順番（`AppLauncher` より前に Isaac 系を import しない）

`isaaclab.app.AppLauncher` で Omniverse アプリを起動する**前**に `gymnasium` や
`isaaclab_tasks` を import すると、拡張がロードされておらず失敗します。
`demo.py` が「引数パース → `AppLauncher` → それ以降に import」という一見不自然な並びなのはこのためです。

### ③ 四元数は (x, y, z, w) 順

Isaac Lab 6.0 の `root_quat_w` は **XYZW** 順です（`QUAT_XYZW_ELEMENT_NAMES`）。
`euler_xyz_from_quat()` も同じ XYZW を前提にしているので、そのまま渡せます。
古い資料の WXYZ 前提のコードをコピーしてくると**向きが壊れます**。

### ④ 速度コマンドは「レンジを潰して再サンプル」で注入する

`CommandManager` の項は乱数でコマンドを引き直す仕組みなので、値を直接代入するAPIがありません。
`ranges` の min = max にして `_resample()` を呼ぶのが実質的な注入手段です。
`_resample()` は**アンダースコア始まりの内部API**なので、バージョン更新で壊れる可能性があります。

あわせて `resampling_time_range = (10000.0, 10000.0)` を入れて、
**環境側が勝手にコマンドを引き直さない**ようにしておく必要があります。

### ⑤ タイムアウトで勝手にリセットされる

既定のエピソード長は 20 秒程度で、(10,10) まで歩く前に打ち切られます。
`episode_length_s = 10000.0` にして実質無限にしています。
同時に `terrain_levels` カリキュラム（地形難易度を上げ下げする仕組み）も
平面地形では意味がないので削除しています。

### ⑥ 旋回ゲインを上げると転ぶ

`target_ang_vel_z` のクリップ値 `±0.45 rad/s` と平滑化係数 `α = 0.08` は、
**転倒しない範囲で最大**という調整値です。ここを緩めると経由地の切り替え時に転びます。
転倒するとエピソードがリセットされ、ルートも最初からやり直しになります。

### ⑦ `simulation_app.update()` の扱い

`demo.py` では最終行の `simulation_app.update()` がコメントアウトされています
（`env.step()` 内でレンダリングが回るため）。`demo2.py` では呼んでいます。
描画が更新されない場合はこの行を復活させてください。

---

## 7. 触ってみると面白いパラメータ

| 変えるもの | どうなるか |
|---|---|
| `waypoints`（コード内） | 経路そのもの。座標を足せば任意のルートを歩かせられる |
| `0.45 * forward_factor` | 前進速度の上限 [m/s]。上げると速いが経由地で曲がりきれなくなる |
| `torch.clamp(..., -0.45, 0.45)` | 旋回速度の上限 [rad/s]。上げると転倒しやすい |
| `filter_alpha = 0.08` | 大きくすると機敏・転びやすい。小さくすると緩慢・ふくらんで曲がる |
| `distance < 0.6` | 到達判定の半径 [m]。小さくすると正確だがその場で回り続けやすい |
| `terrain_type = "plane"` | `"generator"` に戻すと起伏地形になり、歩行ポリシー本来の難易度になる |
| `cone_positions` | コーンの位置（見た目のみ） |

---

## 8. 既知の制限

- **環境数は 1 固定**（`num_envs = 1`）。制御が Python のスカラー演算なので並列化にはテンソル化が必要です。
- コーンに**当たり判定がありません**。経路は人間が事前に与えたもので、回避はしていません。
- 到達判定が距離ベースなので、**行き過ぎたことを検知できません**。
  経由地を大きくオーバーシュートすると戻ろうとして回り込みます
  （軸方向の符号付き距離で判定する方法は `demo5.md` §6 ② を参照）。
- ゴール到達後もループは終わりません（`arrived = True` で停止コマンドを出し続けます）。
- 学習済み `policy.pt` に依存します。観測の次元が違うポリシーを渡すと推論で落ちます。
- `cmd_term._resample()` という内部APIに依存しています。

---

## 9. 参考

- タスク: `IsaacContrib-Velocity-Rough-AnymalC`（`isaaclab_tasks` に登録）
- 学習側のスクリプト: `scripts/reinforcement_learning/rsl_rl/train.py` / `play.py`
- 関連デモ:
  - `demo2.md` — 同じ仕組みで 10 m 四方を巡回させる版
  - `demo5.md` — LiDAR で障害物を実際に検知して避ける版
