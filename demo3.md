# demo3.py — 俯瞰カメラで見つけたキューブを Franka アームでピッキング

Isaac Lab 6.0 系で、**固定視点の RGB-D カメラだけを頼りにキューブの 3D 位置を推定し、
Franka Emika Panda アームで掴んで持ち上げる**デモです。

シミュレータは当然キューブの正確な位置（真値）を知っていますが、このデモは**それを一切使いません**。
カメラ画像から色でキューブを切り出し、深度画像を逆投影して世界座標を自力で求めます。
真値は「推定がどれだけ当たったか」を表示するためだけに読んでいます。

つまり、**実機で成立する情報だけでピッキングを完結させる**のが主題です。

---

## 1. 起動方法

```powershell
# 仮想環境をアクティベート
PS C:\Users\takuy\dev> env_isaaclab\Scripts\activate

# プロジェクトへ移動して実行
PS C:\Users\takuy\dev\IsaacLab> .\isaaclab.bat -p demo3.py --viz kit
```

| やりたいこと | コマンド |
|---|---|
| ビューアで眺める（標準） | `isaaclab.bat -p demo3.py --viz kit` |
| カメラ解像度を上げる | `isaaclab.bat -p demo3.py --viz kit --camera_width 1280 --camera_height 960` |
| エピソードを長くする | `isaaclab.bat -p demo3.py --viz kit --episode_length_s 12` |

> **重要**: このデモは `source/isaaclab/isaaclab/utils/math.py` の `unproject_depth()` に
> **修正を当てないと動きません**（逆投影された点群が壊れます）。§6 ① を必ず参照してください。

初回起動時は Franka の USD をクラウドアセットから取得するのでネットワーク接続が必要です。

### コマンドラインオプション

| オプション | 既定値 | 内容 |
|---|---|---|
| `--position_threshold` | `0.01` | ステートマシンが「到達した」と見なす位置誤差 [m] |
| `--camera_width` | `640` | 俯瞰カメラの画像幅 [px] |
| `--camera_height` | `480` | 俯瞰カメラの画像高さ [px] |
| `--episode_length_s` | `8.0` | エピソード長 [s]。既定の 5 s ではリフトが終わる前に切れる |

`--viz` / `--device` などは Isaac Lab 標準の `AppLauncher` 引数です。
`--enable_cameras` はスクリプト側で既定 ON にしてあります（カメラセンサにはレンダリングが要るため）。

---

## 2. 何をするデモか

ベースは `IsaacContrib-Lift-Cube-Franka-IK-Abs` タスク（`lift_cube_sm.py` と同じ）です。
そこへ俯瞰カメラを足し、**ステートマシンに渡すキューブ位置を真値からカメラ推定に差し替え**ています。

1. 作業台の真上 1.15 m に RGB-D カメラを置き、真下（台の中心）を向ける
2. キューブを**彩度の高い緑**に塗る（色で切り出せるようにするため）
3. アームが REST の間にカメラ画像からキューブ位置を推定し、サンプルを貯める
4. 5 フレーム以上貯まったら**中央値をラッチ**して、その位置めがけてピッキングを開始する
5. 真上へ接近 → 降下 → 把持 → 持ち上げ

### シーンの配置（単位 m、環境ローカル座標）

```
            z
            ^
     1.15   |         📷 俯瞰カメラ (0.5, 0.0, 1.15) 真下向き
            |         |
            |         | 画角: 焦点距離 24 mm / 水平アパーチャ 20.955 mm
            |         | クリップ: 0.01 〜 5.0 m
            |         v
            |   ┌───────────────┐
     0.05   |   │  ■ キューブ    │  公称 (0.5, 0.0, 0.035)
            |   │  (緑に着色)    │  ランダム範囲 x ±0.1 / y ±0.25
     0.00   |===┴═══ 作業台 ═══┴===
            |
       🤖 Franka (原点付近)
            +------------------------------------> x
              0.0        0.5
```

- カメラは**環境原点からの相対位置**で指定し、`set_world_poses_from_view()` で向けています。
- キューブの初期位置はエピソードごとにランダム化されます（x ±0.1 m、y ±0.25 m）。
  カメラの画角はこの範囲が余裕を持って収まる高さに置いてあります。
- ステートマシンのデバッグ表示（座標軸ギズモ）は非表示にして、見た目を素の状態にしています。

### 「見てから動く（look-then-act）」方式

推定を更新するのは**ステートマシンが `REST` にいる間だけ**です。

```
REST ────────────> APPROACH_ABOVE ──> APPROACH ──> GRASP ──> LIFT
  ^                     ^
  |                     |
 ここだけ推定を更新      ここから先は推定を凍結（ラッチ済みの値を使い続ける）
 （5サンプル貯まるまで
   REST に留め置く）
```

アームが降り始めるとグリッパがキューブを遮蔽し、推定はむしろ悪化します。
そのため**掴みにいく前に見て、あとは信じて動く**構成にしています。
サンプルが貯まるまでは `hold_at_rest()` で待機時間をリセットし続け、
**見えていないキューブに手を伸ばさない**ようにしています。

---

## 3. フィジカルAI部分（このデモの中心）

### (1) 色によるセグメンテーション — `segment_green()`

固定しきい値（「緑チャンネルが 0.5 以上」など）ではなく、**正規化色度**で判定します。

```
green_fraction = G / (R + G + B)
cube_mask = (green_fraction > 0.5) かつ (R + G + B > 0.05)
```

こうすると**明るさに依存しません**。グリッパの影に入ったキューブ画素も、
明るく照らされた画素と同じ色度を保ちます。一方で無彩色の作業台は明るさによらず
`green_fraction ≒ 1/3` なので確実に除外されます。
第 2 項は、比が単なるノイズになる**真っ黒に近い画素**を弾くためのものです。

### (2) 深度の逆投影 — `create_pointcloud_from_depth()`

深度画像全体を、カメラの内部パラメータ（`intrinsic_matrices`）と
外部パラメータ（`pos_w` / `quat_w_ros`）を使って世界座標の点群に変換します。

```python
points_w = create_pointcloud_from_depth(
    intrinsic_matrix=camera.data.intrinsic_matrices.torch[0],
    depth=depth_img,
    keep_invalid=True,
    position=camera.data.pos_w.torch[0],
    orientation=camera.data.quat_w_ros.torch[0],
    device=depth_img.device,
)
```

`keep_invalid=True` にして**点の数を画素数と一致させて**おき、マスクと添字で対応づけます。
ただし `unproject_depth()` は (H, W) 画像を**幅の添字が遅く回る順**で平坦化するため、
画素 (v, u) は添字 `u * H + v` に対応します。マスク側も `transpose(0, 1)` してから
`reshape(-1)` する必要があります（§6 ③）。

### (3) 「見えている面」から「中心」を復元する

セグメントされた点をそのまま平均すると**中心にはなりません**。2 つの補正を入れています。

**補正1: 上面だけを使う**

```python
top_of_cube = torch.quantile(cube_points[:, 2], 0.9)
top_face = cube_points[cube_points[:, 2] >= top_of_cube - 0.005]
center_xy = top_face[:, :2].mean(dim=0)
```

シルエット際のアンチエイリアス画素は、**キューブの色と作業台の深度**が混ざっています。
またキューブがカメラ直下から外れていると側面も写ります。
全部を平均すると重心が横にも下にも引っ張られるため、**上面（上から 5 mm のスラブ）に限定**します。

**補正2: 支持面との中点を取る**

```python
support_z = 「中心から半径 0.05〜0.15 m のリングにある非キューブ画素」の z の中央値
cube_height = top_z - support_z
center_z = top_z - 0.5 * cube_height
```

キューブは台に載っているので、**上面と台の中点が中心の高さ**になります。
点群をそのまま平均すると推定が上面に乗り、**キューブ半分ぶん高い位置**を掴もうとして空振りします。
この方法なら**キューブのサイズをハードコードせずに**中心が出ます。

**サニティチェック**: 求まった高さが `0.01 〜 0.2 m` の範囲外なら推定を棄却します。
カメラの外部パラメータと描画フレームがずれていると、ここに引っかかって無効な推定が弾かれます。

### (4) 中央値によるラッチ

1 フレームの推定はノイズを含むので、REST 中に貯めたサンプルの**中央値**を採用します
（平均ではなく中央値なので、たまに混じる外れ値に強い）。
リセット直後の `vision_warmup_frames = 10` フレームは、レンダラが物理に追いついておらず
**リセット前のキューブが写っている可能性がある**ため捨てます。

### (5) ピック&リフトのステートマシン（Warp カーネル）

`lift_cube_sm.py` と同じ構成です。GPU 上の Warp カーネルで状態遷移を計算します。

| 状態 | 目標 EE 位置 | グリッパ | 次へ進む条件 |
|---|---|---|---|
| `REST` | 現在位置を維持 | 開 | 0.2 s 経過 |
| `APPROACH_ABOVE_OBJECT` | キューブの 0.1 m 上 | 開 | 到達 ＋ 0.6 s |
| `APPROACH_OBJECT` | キューブ中心 | 開 | 到達 ＋ 0.6 s |
| `GRASP_OBJECT` | キューブ中心 | 閉 | 0.3 s 経過 |
| `LIFT_OBJECT` | コマンドの目標姿勢 | 閉 | 到達 ＋ 1.0 s（最終状態） |

到達判定は `--position_threshold`（既定 0.01 m）です。
アクションは**絶対 IK**（位置 3 ＋ 四元数 4 ＋ グリッパ 1 の計 8 次元）で与えます。

---

## 4. 実行したときのログ

起動時:

```
[INFO] シミュレーション開始。俯瞰カメラの映像からキューブ位置を推定してピッキングします...
[INFO] カメラ解像度: 640x480 px
```

推定をラッチしてピッキングを開始した時点で 1 回だけ:

```
[VISION] 推定位置(env-local): [0.503 0.118 0.034] / 真値との誤差: 4.2 mm (12 フレームの中央値)
```

`真値との誤差` は推定がどれだけ当たったかの指標です（**制御には使っていません**）。
エピソードがリセットされるたびに、新しいキューブ位置に対してこの行が再度出ます。

終了はウィンドウを閉じるか `Ctrl+C` です（ループに終了条件はありません）。

---

## 5. コードの構成

| 要素 | 役割 |
|---|---|
| `add_overhead_camera()` | 作業台を見下ろす RGB-D カメラをシーンに追加 |
| `colorize_cube_for_vision()` | キューブを彩度の高い緑（`roughness=0.9`, `metallic=0.0`）に着色 |
| `hide_debug_markers()` | 座標軸ギズモを消して見た目を素にする |
| `patch_franka_usd_path()` | Franka の USD パスを `Legacy/` 配下に差し替え（§6 ②） |
| `segment_green()` | 正規化色度による色セグメンテーション |
| `estimate_cube_position_from_camera()` | **RGB-D からキューブ中心を推定する本体** |
| `infer_state_machine()` (Warp カーネル) | ピック&リフトの状態遷移 |
| `PickAndLiftSm` | ステートマシンのラッパ（`compute` / `reset_idx` / `hold_at_rest`） |
| `main()` | セットアップとメインループ |

メインループの 1 ステップはこの順です:

```
env.step(actions) → EE 姿勢を読む
  → REST 中なら カメラ推定を 1 サンプル追加（5 個未満なら REST に留め置く）
  → 5 個以上なら 中央値をラッチ
  → ステートマシンを進めて 次のアクションを作る
  → リセット検知なら サンプルを捨てて やり直し
```

キューブの着色・マーカー非表示・USD パッチはすべて**ローカルの `env_cfg` 書き換え**で行っており、
共有アセット設定（`FRANKA_PANDA_CFG` など）には触っていません。

---

## 6. ハマりどころ（実装メモ）

### ① `unproject_depth()` の同次座標バグ（**修正必須**）

`source/isaaclab/isaaclab/utils/math.py` の `unproject_depth()`（1246 行目付近）を修正しないと、
**逆投影された点群が完全に壊れます**。

```diff
-    pixels = torch.nn.functional.pad(img_indices, (0, 0, 1, 0), mode="constant", value=1.0)
+    # append the homogeneous coordinate so each column is (u, v, 1); the normalization below divides
+    # by the last row, which must therefore be the homogeneous one and not the v index
+    pixels = torch.nn.functional.pad(img_indices, (0, 0, 0, 1), mode="constant", value=1.0)
```

元のコードでは各列が `(1, u, v)` になっていました。しかし直後の

```python
points = points / points[:, -1, :].unsqueeze(1)  # normalize by last coordinate
```

は**最終行が同次座標の 1 であること**を前提に割り算しているため、`v`（縦画素インデックス）で
割ってしまいます。`(u, v, 1)` の順にパディングすることで、`K⁻¹ @ (u, v, 1)ᵀ` という
正しいピンホール逆投影になります。

`demo3.py` は `create_pointcloud_from_depth()` → `unproject_depth()` 経由で
深度画像を世界座標へ逆投影しているので、**この修正なしでは視覚推定がまったく機能しません**。

### ② Franka の USD が 404 になる

`FRANKA_PANDA_CFG` は `Robots/FrankaEmika/panda_instanceable.usd` を指していますが、
Isaac 6.0 のコンテンツサーバでは**このファイルが `Legacy/` 配下へ移動**しています。
`patch_franka_usd_path()` でローカルの `env_cfg` 側のパスだけを差し替えています
（共有アセット設定は触りません）。

### ③ カメラの姿勢をスクリプトから変えるなら `update_latest_camera_pose=True`

`set_world_poses_from_view()` でカメラを向けても、この設定がないと
**`camera.data.pos_w` が spawn 時の姿勢を返し続けます**。
画像は新しい姿勢で描かれているのに外部パラメータは古いままになり、逆投影が意味をなしません。

### ④ 点群の平坦化順序はマスクと一致しない

`unproject_depth()` は (H, W) 画像を**幅の添字が遅く回る順**で平坦化します
（画素 (v, u) → 添字 `u * H + v`）。マスクをそのまま `reshape(-1)` すると対応がずれて、
**画像の別の場所を掴みにいきます**。`mask.transpose(0, 1).reshape(-1)` が正解です。

### ⑤ 推定を「動き出したあとも更新」してはいけない

アームが降下するとグリッパがキューブを覆い、緑画素が減って推定が横へ流れます。
その値を追いかけるとアームが目標を追い越し続けて掴めません。
**REST 中だけ更新し、動き出したら凍結**するのが要点です。

### ⑥ リセット直後の数フレームは捨てる

レンダラは物理より遅れるので、リセット直後の画像には**リセット前の位置のキューブ**が写っています。
`vision_warmup_frames = 10` フレームぶん捨ててから採取を始めます。
同じ理由で、カメラを向けた直後にも `env.step()` を 12 回空回ししてレンダラを追いつかせています。

### ⑦ 点群をそのまま平均すると半分ぶん高い位置を掴む

見えているのは上面だけなので、平均すると中心ではなく**上面**が出ます。
グリッパがキューブの上で空振りする典型的な失敗です。支持面との中点を取る補正が必要です（§3 (3)）。

### ⑧ キューブのマテリアルは「粗く・非金属」にする

`roughness` を下げたり `metallic` を上げたりすると鏡面ハイライトで**色が白く飛び**、
その画素が色度判定を通らなくなってマスクが痩せます。`roughness=0.9, metallic=0.0` にしてあります。

### ⑨ 既定のエピソード長ではリフトが完了しない

既定は 5 秒で、持ち上げ切る前にリセットされます。`--episode_length_s`（既定 8 秒）で延ばしています。

---

## 7. 触ってみると面白いパラメータ

| 変えるもの | どうなるか |
|---|---|
| `--camera_width` / `--camera_height` | 解像度。上げると推定精度が上がるが描画が重くなる（4:3 を保てば画角は不変） |
| `--position_threshold` | 小さくすると丁寧に位置合わせするが、到達判定が通らず止まることがある |
| `CAMERA_EYE`（コード内） | カメラの高さ・位置。低くすると精度は上がるがアームに遮られやすくなる |
| `min_chroma = 0.5`（`segment_green`） | 下げるとマスクが太って台まで拾い、上げると影の部分を取りこぼす |
| `top_face_tolerance = 0.005` | 上面とみなすスラブの厚み [m]。厚くすると側面を巻き込む |
| `min_vision_samples = 5` | 増やすと推定が安定するが、動き出すまでの待ちが長くなる |
| `colorize_cube_for_vision()` の色 | 赤や青に変えるなら `segment_green()` も対応するチャンネルに変更が必要 |

---

## 8. 既知の制限

- **環境数は 1 固定**（`num_envs=1`）。視覚パイプラインが `[0]` 番目を直接触っています。
- 色セグメンテーションは**単色の物体が 1 個だけ**写っている前提です。
  同じ色のものが複数あると重心が中間に出ます（連結成分の分離はしていません）。
- 支持面の計測は「キューブの周囲 0.05〜0.15 m のリングが平らな台である」ことを前提にしています。
- 推定はエピソード開始時に一度ラッチするだけで、**追従（トラッキング）はしません**。
  掴んだあとにキューブが動いても再推定しません。
- ループに終了条件がありません。エピソードは `--episode_length_s` ごとにリセットを繰り返します。
- `math.py` の修正が未コミットの作業ツリー変更として必要です（§6 ①）。

---

## 9. 参考

- ベースタスク: `IsaacContrib-Lift-Cube-Franka-IK-Abs`
- 元になった Isaac Lab のサンプル:
  - `scripts/environments/state_machine/lift_cube_sm.py`（Warp ステートマシン部分）
  - `scripts/tutorials/04_sensors/add_sensors_on_robot.py`（`Camera` の使い方）
- 関連デモ:
  - `demo4.md` — このデモを mp4 に録画する版
  - `demo5.md` — LiDAR で障害物を避けながら搬送する AGV のデモ
